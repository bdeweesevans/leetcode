class Solution:
    def isPalindrome(self, x: int) -> bool:
        x = str(x)
        palindrome = True
        for i in range(len(x)):
            if x[::1] != x[::-1]:
                palindrome = False
                break         
        return palindrome
